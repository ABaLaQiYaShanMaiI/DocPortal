# Runtime dependencies (synced with pyproject.toml)
# For `pip install -r requirements.txt`
# python-magic is optional — only install on Linux/macOS where libmagic is available.
# On Windows, extension-based dispatch is used as fallback.
pdfminer.six>=20221105
python-docx>=0.8.11
python-pptx>=1.0.2
openpyxl>=3.1.5
chardet>=5.0

# ── Optional: Drag-and-drop support for GUI ──
# Uncomment the line below to enable folder drag-and-drop in the GUI.
# If installation fails (needs a C compiler on Windows), the GUI will still
# work fine — just use the "Browse" button or Ctrl+V paste to load folders.
# tkinterdnd2>=0.3.0

# ── Dev dependencies ──
# pytest>=7.0.0
